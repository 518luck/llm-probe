version: '3.8'

services:
  llm-probe:
    build: .
    container_name: llm-probe
    ports:
      - "3002:3001"
    volumes:
      - probe_data:/app/app/data
    restart: unless-stopped

volumes:
  probe_data:
